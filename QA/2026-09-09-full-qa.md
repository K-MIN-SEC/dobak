# 2026-09-09 전체 QA 결과

## 결론

- 대상 브랜치: `0908`
- Unity 에디터 플레이 모드 전체 루트: **13/13 PASS**
- 최종 실행 실패: **0건**
- 최종 실행 주요 런타임 예외: **0건**
- 16:9 / 16:10 시각 스모크 QA: **PASS**
- ScenarioV5 정적 데이터 검사: **PASS**

## 실행한 루트

1. Recovery
2. Prevention
3. NoGamble
4. NoHelp
5. NoFunds
6. MinjaeDebt
7. SeojunDebt
8. LoanHeld
9. RepeatLoss
10. ProjectFail
11. MixedA
12. MixedB
13. MixedC

각 루트는 1일차 시작부터 5일차 엔딩까지 Unity 에디터 플레이 모드에서 실제 앱 진입, 지도 이동, 공부 문제 풀이, 메시지 선택, 도박 실행, 차용, 취침 순서로 진행했다. 혼합 루트는 선택지를 서로 다르게 조합해 단일 권장 선택만 반복하지 않았다.

## 최종 데이터 검사

- CSV 행: 173
- 장면: 93
- 선택지 ID: 37
- 중복 line_id: 0
- 중복 choice_id: 0
- 존재하지 않는 장면 참조: 0
- 사용 날짜 범위: 1~5일차
- 기존 10~14일차 문구: 0
- 문제 문구 `사례 속`, `조기 취침`, `집으로 가는 중`, `학교로 가는 중`: 0

## 확인한 핵심 흐름

- 첫날 도박을 하지 않은 루트가 이후 도박 경험자로 잘못 처리되지 않는다.
- 학교, 공부, 알바, 미처리 메시지가 우선인 동안 도박 앱이 실행되지 않는다.
- 연속 도박 시 홈으로 빠지지 않고 선택한 다음 도박 장면이 이어진다.
- 무자금 상태에서 차용을 거절해도 다음 진행이 막히지 않는다.
- 엄마와 서준에게 이미 빌렸다면 이후 선택지에서 두 사람은 제외되고 민재 또는 빌리지 않음만 나온다.
- 민재에게 거절한 뒤에도 다음 무자금 기회에서 민재 제안이 다시 발생한다.
- 22시 전 차용은 즉시 대상을 고르고, 22시 이후 차용은 늦었다는 독백과 정상 아침 장면 뒤에 대상을 고른다.
- 지각 안내는 지도 앱 안에서 표시되고 학교 스토리는 지각 여부와 관계없이 이어진다.
- 도박 때문에 카페에 가지 못한 경우 점장 메시지, 결근 처리, 후속 대사가 연결된다.
- 5일 캠페인 중간에 일정 실패만으로 조기 엔딩이 발생하지 않는다.
- 플레이어가 답하는 것이 자연스러운 메시지는 선택 답장 또는 후속 발신으로 마무리된다.
- 메시지 선택은 같은 상태에서 중복 제출되지 않으며 주인공 발신 말풍선이 실제로 생성된다.
- 수신 메시지는 타이핑 지연과 읽기 간격을 거쳐 순서대로 표시된다.

## 이번 QA에서 수정한 문제

- 무도박 루트 1일차 저녁 장면이 재진입 상태 때문에 멈추던 문제
- 일정 실패가 누적되면 3일차에 5일 시나리오가 조기 종료되던 문제
- 태블릿 선택지가 연속으로 열릴 때 이전 선택지 정리가 새 선택지 상태까지 지우던 문제
- 처음 메시지를 읽기 전 공부 앱 안내에서 아직 소개되지 않은 민재 이름이 노출되던 문제
- 차용 대상 이력과 상환 선택 때문에 반복 손실 QA가 잘못된 대상을 누르던 검증 문제
- 16:9에서 앱 창 오른쪽으로 홈 화면과 아이콘이 비치던 고정 폭 문제
- 시각 QA와 전체 QA가 Unity 모드 전환 중 다음 단계를 잃던 자동 실행 문제

## 시각 검증

다음 화면을 1920x1080(16:9), 1920x1200(16:10)으로 각각 렌더링해 확인했다.

- 타이틀: `할래말래`, `게임하기`, `중랑경찰서`
- 1일차 첫 비주얼노벨 장면
- 태블릿 홈
- 은행
- 지도
- 메시지
- 공부
- 취침

상단 상태 바와 하단 내비게이션 바가 두 비율 모두 잘리지 않는다. 16:9에서도 앱 배경이 전체 가로폭을 덮어 홈 아이콘이 앱 위로 노출되지 않으며, 공부 선택지 세 개가 화면 안에 유지된다.

캡처 위치: `Logs/ScenarioV4VisualQa`

## 로그와 제한 사항

- 전체 재검증 시작 로그 위치: `Logs/QA-0909-editor.log` 21798행 이후
- 해당 구간 `NullReferenceException`, `InvalidOperationException`, coroutine inactive 오류, C# 컴파일 오류, 인덱스/참조 오류: 0건
- 이번 검증은 Unity 에디터 플레이 모드에서 수행했다.
- WebGL 및 Android 최종 빌드 파일을 새로 생성해 기기에서 설치·실행하는 검증은 이번 실행에 포함하지 않았다. WebGL 도박 런처는 단일 클릭 경로로 통합된 현재 코드를 에디터에서 검증했다.
